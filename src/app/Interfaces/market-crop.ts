import { StringifyOptions } from 'querystring';

export interface MarketCrop {

    uid:string;//stores user id that post the crop
    name:string;
    amount:string;//stores the amout of crop the user wants to add/ amount available
}
